package tqs.sparkflow.userservice.dto;

/**
 * Data Transfer Object for user registration requests.
 * Extends BaseUserDto to inherit common user fields and validation.
 */
public class RegisterDto extends BaseUserDto {
  // Inherits username, email, password fields and their validations from BaseUserDto
  // No additional fields needed for registration
}